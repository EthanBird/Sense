<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!--
          Debug-only host: exported so local shell-driven device tests can prelaunch it on OEM
          builds that reject an instrumentation process's first background Activity start.
        -->
        <activity
            android:name="io.github.ethanbird.senseime.ui.SkillKeyboardTestActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
